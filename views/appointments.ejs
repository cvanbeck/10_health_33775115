<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet"  type="text/css" href="/main.css" />
    <title>Appointments</title>
</head>
<body>
    <%-include("header") %>
    <h1>All appointments</h1>
    <% appointments.forEach(appointment => {%>
        <ul>
            <li>Patient Name: <%=appointment.pfirst %> <%=appointment.plast %> </li>
            <li>Doctor Name: <%=appointment.dfirst %> <%=appointment.dlast %>, <%=appointment.department %> department</li>
            <li>Appointment is at: <%=appointment.time.slice(0, 5) %> on <%=appointment.date.toString().slice(0, 10) %></li>
        </ul>
    <%})%>
</body>
</html>